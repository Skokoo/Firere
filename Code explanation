1.Macros = it is "Efficient" if used corrrectly, yet... "Correctly".

2.Loop #1(Randoma) = it make the "char" summoned from the bottom, and ,it is included a fuel to!

3.Loop #2(UP1, UP2) = Make the fire goes up, include more things like direction, heat pixels.

4.Loop #3(Gradi) = Colors(Blue), not using "smooth" color gradient for efficiency, and it would make my phone crash! 






