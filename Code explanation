1.Macros = it is "Efficient"if used corrrectly, but it make the code obfuscated beacuse i like it!

2.Loop #1(Randoma) = "summoning machine", it make the "char summoned", and,it have a fuel to!

3.Loop #2(UP1, UP2) = Make the fire goes up, include more things like direction, heat pixels.
like int DERICTION, int HEATP, int DEST, int LASTED

4.Loop #3(Gradi) = Colors(Blue), not using "smooth" color gradient for efficiency, and it would make my device crash.






